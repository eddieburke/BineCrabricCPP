#pragma once

// Forward declarations only — no using aliases.
// Use BlockTypes.hpp when net::minecraft::* aliases are required.

namespace net::minecraft::block {

class Block;
struct BlockDefinition;
class BlockRegistry;

void initializeBlocks();

} // namespace net::minecraft::block
